function THETA = SGD(X,Y,t_max,lambda,s_t)
    [d n] = size(X);
    m = length(unique(Y));
    %initialize THETA to zeros
    THETA = zeros((d + 1),m); %(d + 1) x m
    for t = 1:t_max
        %choose sample index: j uniformly at random from {1,...,n}
        j = randi(n,1,1);
        %compute gradients
        for 
    end
end